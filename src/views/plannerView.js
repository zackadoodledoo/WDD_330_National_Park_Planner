//plannerView.js 

export async function renderPlanner(root, params) {
  root.innerHTML = `
    <section class="panel">
      <h1>Trip Planner</h1>
      <p>Planner view is working.</p>
      <a href="#/">← Back to Discover</a>
    </section>
  `;
}
