// detailView.js file

export async function renderDetail(root, params) {
  root.innerHTML = `
    <section class="panel">
      <h1>Park Details</h1>
      <p>This page is working.</p>
      <a href="#/">← Back to Discover</a>
    </section>
  `;
}
